{{! signers.handlebars - for each signer, show the age and city (if they exist) and if the url exists, link up the name (make sure the url does not run Javascript code). Cities, when shown, should link to a new url }}

<main>

    <h1>Signers Page</h1>
    {{#if city}}
        Please find below all signers from
        {{this.city}}
    {{/if}}

    <ul>
        {{#if displayCity}}
            {{#signers}}
                <li>
                    <a href="{{this.url}}" target="_blank">{{this.first}}
                        {{this.last}}</a>
                    {{this.age}}
                    <a href="/signers/{{this.city}}">{{this.city}}</a>
                </li>
            {{/signers}}
        {{else}}
            {{#signers}}
                <li>
                    <a href="{{this.url}}" target="_blank">{{this.first}}
                        {{this.last}}</a>
                    {{this.age}}
                </li>
            {{/signers}}
        {{/if}}
    </ul>

</main>